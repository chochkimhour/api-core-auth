# Express TypeScript Example

This example shows ESM TypeScript usage with typed imports from `api-core-auth`.

```bash
npm install
JWT_SECRET=replace-with-a-long-random-secret npm run dev
```

The demo password hash is for `Password123`.
